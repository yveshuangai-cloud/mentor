# Railway CLI uploads this directory when running `railway up`.
# Keep everything the Dockerfile copies (packages, assets/aieq, assets/ai-personality, output/design, soul).
# Leave out planning documents and office files: they are large and never used at runtime.
/docs/
/output/documents/
/output/pdf/
/output/*.pptx
# PNG design masters: the app serves the JPEG next to each one.
/assets/ai-personality/scenes/**/*.png
/output/design/ai-personality-share-cards-16/*.png
/assets/aieq/animals/swiss-modernist/*.png
