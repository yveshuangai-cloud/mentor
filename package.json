{
  "name": "mantou-platform",
  "private": true,
  "version": "0.1.0",
  "description": "饅頭語氣靈 AI 平台（多租戶 SaaS on LINE）",
  "type": "module",
  "workspaces": [
    "packages/*",
    "packages/voice-call/frontend"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm run build -w packages/backend",
    "dev": "npm run dev -w packages/backend",
    "test": "npm run test -w packages/backend"
  }
}
